============
Installation
============

At the command line::

    $ easy_install djangocms-rest-view

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangocms-rest-view
    $ pip install djangocms-rest-view
